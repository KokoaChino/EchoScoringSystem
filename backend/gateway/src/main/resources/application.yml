server:
  port: 4000
spring:
  application:
    name: gateway
  profiles:
    include: route
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        init-timeout: 30000
        failure-tolerance-enabled: true
logging:
  level:
    org.springframework.cloud.gateway: INFO